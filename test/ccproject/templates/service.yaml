apiVersion: v1
kind: Service
metadata:
  name: {{ template "ccproject.fullname" . }}
  labels:
    app: {{ template "ccproject.name" . }}
    chart: {{ template "ccproject.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ccproject.name" . }}
    release: {{ .Release.Name }}
